#include<stdio.h>
#include<stdlib.h>

typedef struct pass
{
    char name[100];
    int age;
}pass;

typedef bus
{
    int type;
    char name[100];
    int seatW;
    int seatN;
}bus;


void display(int n)
{
    int num;
    //Take the details of bus from the file later on
    if(bus.type == n)
    {
        //int num;

    }
}

int main()
{
    //get all the info from the file later on
    printf("Services are from\n1.Kolkata to Chennai\n2.Vellore to Chennai\n3.Kolkata to Bangalore\nSelect desired number\n");
    int n;
    scanf("%d",&n);
}